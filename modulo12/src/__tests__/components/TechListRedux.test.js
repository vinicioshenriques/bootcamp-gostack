import React from "react";
import { render, fireEvent, cleanup } from "@testing-library/react";
import TechList from "~/components/TechListRedux";

describe("TechListRedux component", () => {
  it("should sum 1 plus 1 and return 2", () => {
    expect(1 + 1).toBe(2);
  });
});
